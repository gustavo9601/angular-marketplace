import { OwlCarouselDirective } from './owl-carousel.directive';

describe('OwlCarouselDirective', () => {
  it('should create an instance', () => {
    const directive = new OwlCarouselDirective();
    expect(directive).toBeTruthy();
  });
});
