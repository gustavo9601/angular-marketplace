import { SlickDirective } from './slick.directive';

describe('SlickDirective', () => {
  it('should create an instance', () => {
    const directive = new SlickDirective();
    expect(directive).toBeTruthy();
  });
});
